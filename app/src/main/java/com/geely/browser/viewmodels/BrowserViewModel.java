package com.geely.browser.viewmodels;

import androidx.lifecycle.ViewModel;
// This ViewModel can be used to store state specific to a single WebView/tab instance
// For example, scroll position, zoom level if needed to be restored, etc.
// For now, it's basic.
public class BrowserViewModel extends ViewModel {
    // Add LiveData or state variables for a single browser tab if needed
    public BrowserViewModel() {
        // Initialization
    }
}
